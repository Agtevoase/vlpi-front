import Choise from 'common/choise';
import React, { Fragment } from 'react';
import { Choice } from 'types/models';
import s from './styles.module.css'

interface Props {
  exercise: Choice[] | null
}

const ChoiseList: React.FC<Props> = ({ exercise }) => {
  return null;
}

export default ChoiseList;
